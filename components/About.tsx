'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react';
const founders = [{ name: 'Jani Karkulahti', role: 'FOUNDER', description: 'Jani is an AI engineer specializing in automation, workflows, and system architecture, with a proven track record of building scalable technology platforms.', initials: 'JK' }, { name: 'Santeri Koskinen', role: 'CO-FOUNDER', description: 'Santeri is a serial entrepreneur with a proven track record of building successful startup and leading the strategic vision behind growing businesses.', initials: 'SK' }];
export default function About() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section ref={ref} className="relative py-24 overflow-hidden"><div className="max-w-7xl mx-auto px-6"><div className="grid lg:grid-cols-2 gap-16 items-start"><motion.div initial={{ opacity: 0, x: -30 }} animate={isInView ? { opacity: 1, x: 0 } : {}} transition={{ duration: 0.6 }}><div className="inline-block px-4 py-1.5 glass-red rounded-full mb-6"><span className="text-sm font-medium text-synabs-red">About</span></div><h2 className="font-display font-bold text-4xl md:text-5xl mb-6">Agents get smarter with<br /><span className="gradient-text">every conversation</span></h2><p className="text-lg text-gray-300 leading-relaxed">We believe customer conversations are one of the most overlooked growth opportunities. That's why we built evolving AI agents that learn, adapt, and actually understand what customers need instead of just responding to them.</p></motion.div><div className="space-y-6">{founders.map((founder, index) => (<motion.div key={founder.name} initial={{ opacity: 0, x: 30 }} animate={isInView ? { opacity: 1, x: 0 } : {}} transition={{ duration: 0.6, delay: index * 0.2 }} className="glass rounded-2xl p-6 hover:border-synabs-red/30 transition-all"><div className="flex items-start gap-4"><div className="w-16 h-16 rounded-2xl bg-gradient-to-br from-synabs-red to-synabs-crimson flex items-center justify-center text-white font-bold text-xl flex-shrink-0">{founder.initials}</div><div className="flex-1"><h3 className="font-display font-bold text-xl mb-1">{founder.name}</h3><div className="text-sm text-synabs-red font-semibold mb-3">{founder.role}</div><p className="text-gray-400 leading-relaxed text-sm">{founder.description}</p></div></div></motion.div>))}</div></div></div></section>); }
