'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react';
export default function CTA() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section ref={ref} className="relative py-24 overflow-hidden"><div className="absolute inset-0 circuit-bg" /><div className="absolute inset-0 hex-bg" /><div className="relative max-w-5xl mx-auto px-6 text-center"><motion.div initial={{ opacity: 0, scale: 0.95 }} animate={isInView ? { opacity: 1, scale: 1 } : {}} transition={{ duration: 0.6 }}><div className="inline-block px-4 py-1.5 glass-red rounded-full mb-6"><span className="text-sm font-medium text-synabs-red">START THE REVOLUTION</span></div><h2 className="font-display font-bold text-4xl md:text-5xl lg:text-6xl mb-6">Stop managing chatbots,<br /><span className="gradient-text">start growing with AI.</span></h2><div className="flex flex-col sm:flex-row gap-4 justify-center mt-10"><a href="#demo" className="px-8 py-4 glass text-white font-semibold rounded-lg hover:bg-white/5 transition-colors">Get a demo</a><a href="#get-started" className="btn-primary px-8 py-4 bg-white text-black font-semibold rounded-lg hover:bg-gray-200 transition-colors">Try it free 14 days</a></div></motion.div></div></section>); }