'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react'; import { RefreshCw, Clock, UserCheck, Calendar, Globe, FileText } from 'lucide-react';
const capabilities = [{ icon: RefreshCw, title: 'Evolves every week', description: 'Reviews every conversation, finds what drives conversions, and updates its own responses. No manual tuning.' }, { icon: Clock, title: 'Never misses a lead', description: 'Responds in under 2 seconds, day or night. Every visitor gets an answer before they leave.' }, { icon: UserCheck, title: 'Lead qualification', description: 'Asks the right questions, captures contact details, and scores leads without a human in the loop.' }, { icon: Calendar, title: 'Calendly booking', description: 'Books meetings directly into your calendar. Lead captured, qualified, and booked in one conversation.' }, { icon: Globe, title: 'Language detection', description: 'Detects the visitor's language and switches instantly. No config needed. Works across every market.' }, { icon: FileText, title: 'Monthly reports', description: 'Clear monthly summaries of conversations, captured leads, and agent insights, so you can track ROI with confidence.' }];
export default function Capabilities() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section id="features" ref={ref} className="relative py-24 overflow-hidden"><div className="max-w-7xl mx-auto px-6"><motion.div initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6 }} className="mb-16"><div className="inline-block px-4 py-1.5 glass-red rounded-full mb-4"><span className="text-sm font-medium text-synabs-red">Capabilities</span></div><h2 className="font-display font-bold text-4xl md:text-5xl lg:text-6xl">Convert more visitors,<br /><span className="gradient-text">automatically</span></h2></motion.div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">{capabilities.map((cap, index) => (<motion.div key={cap.title} initial={{ opacity: 0, y: 30 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6, delay: index * 0.1 }} className="glass rounded-2xl p-8 hover:border-synabs-red/30 transition-all group h-full"><div className="w-12 h-12 rounded-xl bg-synabs-red/10 border border-synabs-red/20 flex items-center justify-center mb-6 group-hover:bg-synabs-red/20 transition-colors"><cap.icon className="w-6 h-6 text-synabs-red" /></div><h3 className="font-display font-bold text-xl mb-3">{cap.title}</h3><p className="text-gray-400 leading-relaxed">{cap.description}</p></motion.div>))}</div></div></section>); }