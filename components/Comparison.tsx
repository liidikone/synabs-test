'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react'; import { Check, X } from 'lucide-react';
const rows = [{ feature: 'First response', regular: 'Answers the question', synabs: 'Answers and guides the visitor forward' }, { feature: 'Lead capture', regular: 'None, or a separate form', synabs: 'Asks and collects details naturally in conversation' }, { feature: 'Sales intent', regular: 'Does not recognise buying signals', synabs: 'Identifies purchase intent and acts on it' }, { feature: 'Follow-up', regular: 'No memory between conversations', synabs: 'Learns from data and improves' }, { feature: 'Booking', regular: 'Not supported', synabs: 'Books meetings directly via calendar integration' }, { feature: 'Optimisation', regular: 'Manual updates only', synabs: 'Improves automatically based on real data' }];
export default function Comparison() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section ref={ref} className="relative py-24 overflow-hidden"><div className="max-w-7xl mx-auto px-6"><motion.div initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6 }} className="mb-16 text-center"><div className="inline-block px-4 py-1.5 glass-red rounded-full mb-4"><span className="text-sm font-medium text-synabs-red">Comparison</span></div><h2 className="font-display font-bold text-4xl md:text-5xl lg:text-6xl">The next generation of<br /><span className="gradient-text">customer engagement</span></h2></motion.div><motion.div initial={{ opacity: 0, y: 30 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6, delay: 0.2 }} className="glass rounded-2xl overflow-hidden"><div className="grid grid-cols-12 border-b border-white/10 bg-white/5"><div className="col-span-4 md:col-span-3 p-6 font-display font-bold text-lg">Situation</div><div className="col-span-4 md:col-span-4 p-6 text-center font-display font-bold text-gray-400">Regular chatbot</div><div className="col-span-4 md:col-span-5 p-6 text-center font-display font-bold text-synabs-red bg-synabs-red/5">Synabs AI agent</div></div>{rows.map((row, index) => (<div key={row.feature} className={`grid grid-cols-12 border-b border-white/5 hover:bg-white/5 transition-colors ${index % 2 === 0 ? '' : 'bg-white/[0.02]'}`}><div className="col-span-4 md:col-span-3 p-6 font-medium">{row.feature}</div><div className="col-span-4 md:col-span-4 p-6 text-center text-gray-400 flex items-center justify-center gap-2"><X className="w-4 h-4 text-gray-600 flex-shrink-0" /><span className="text-sm">{row.regular}</span></div><div className="col-span-4 md:col-span-5 p-6 text-center bg-synabs-red/5 flex items-center justify-center gap-2"><Check className="w-4 h-4 text-synabs-red flex-shrink-0" /><span className="text-sm font-medium">{row.synabs}</span></div></div>))}</motion.div></div></section>); }