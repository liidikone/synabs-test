'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react'; import { Zap, Target, TrendingUp } from 'lucide-react';
const stats = [{ value: '<2s', label: 'Average response time for every conversation', icon: Zap }, { value: '96%', label: 'Of conversations handled without human escalation', icon: Target }, { value: '+23%', label: 'Average increase in qualified leads across early testers', icon: TrendingUp }];
export default function EarlyResults() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section ref={ref} className="relative py-24 overflow-hidden"><div className="max-w-7xl mx-auto px-6"><motion.div initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6 }} className="mb-16"><div className="inline-block px-4 py-1.5 glass-red rounded-full mb-4"><span className="text-sm font-medium text-synabs-red">Early results</span></div><h2 className="font-display font-bold text-4xl md:text-5xl lg:text-6xl">Numbers that<br /><span className="gradient-text">move the needle</span></h2></motion.div><div className="grid md:grid-cols-3 gap-6">{stats.map((stat, index) => (<motion.div key={stat.label} initial={{ opacity: 0, y: 30 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6, delay: index * 0.15 }} className="glass rounded-2xl p-8 hover:border-synabs-red/30 transition-all group"><stat.icon className="w-8 h-8 text-synabs-red mb-6 group-hover:scale-110 transition-transform" /><div className="font-display font-bold text-5xl md:text-6xl mb-3 gradient-text">{stat.value}</div><p className="text-gray-400 leading-relaxed">{stat.label}</p></motion.div>))}</div><motion.div initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6, delay: 0.6 }} className="mt-12 glass rounded-2xl p-8 border-l-4 border-synabs-red"><p className="text-lg md:text-xl text-gray-200 leading-relaxed mb-3">Companies that respond to leads within one hour are nearly <span className="text-synabs-red font-bold">7× more likely</span> to qualify them compared to those that wait just two hours.</p><p className="text-sm text-gray-500">Harvard Business Review · Speed-to-lead study</p></motion.div></div></section>); }