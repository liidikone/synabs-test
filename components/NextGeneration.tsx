'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react'; import { Upload, BarChart3, Brain } from 'lucide-react';
const steps = [{ step: 'S1', title: 'Trained on your business from day one', description: 'Share your website, documents, and FAQs with us. We upload everything and configure the agent to understand your products, brand voice, and customer questions.', icon: Upload }, { step: 'S2', title: 'Detects patterns that lead to conversions', description: 'Analyzes conversation history to identify common buying signals and recommend higher-converting responses.', icon: BarChart3 }, { step: 'S3', title: 'Predicts intent and acts before you ask', description: 'Over time, the agent learns visitor behavior, predicts their needs, and identifies the best opportunities to present your services and guide them toward a decision.', icon: Brain }];
export default function NextGeneration() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section id="how-it-works" ref={ref} className="relative py-24 overflow-hidden"><div className="absolute inset-0 red-grid opacity-30" /><div className="relative max-w-7xl mx-auto px-6"><motion.div initial={{ opacity: 0, y: 20 }} animate={isInView ? { opacity: 1, y: 0 } : {}} transition={{ duration: 0.6 }} className="mb-16"><div className="inline-block px-4 py-1.5 glass-red rounded-full mb-4"><span className="text-sm font-medium text-synabs-red">Next generation</span></div><h2 className="font-display font-bold text-4xl md:text-5xl lg:text-6xl">Evolves every week,<br /><span className="gradient-text">outplays competitors</span></h2></motion.div><div className="space-y-6">{steps.map((step, index) => (<motion.div key={step.step} initial={{ opacity: 0, x: index % 2 === 0 ? -30 : 30 }} animate={isInView ? { opacity: 1, x: 0 } : {}} transition={{ duration: 0.6, delay: index * 0.2 }} className="glass rounded-2xl p-8 md:p-12 hover:border-synabs-red/30 transition-all group"><div className="grid md:grid-cols-12 gap-8 items-center"><div className="md:col-span-2"><div className="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-synabs-red/10 border border-synabs-red/30 group-hover:bg-synabs-red/20 transition-colors"><span className="font-display font-bold text-2xl text-synabs-red">{step.step}</span></div></div><div className="md:col-span-2 hidden md:flex justify-center"><step.icon className="w-12 h-12 text-synabs-red group-hover:scale-110 transition-transform" /></div><div className="md:col-span-8"><h3 className="font-display font-bold text-2xl md:text-3xl mb-4">{step.title}</h3><p className="text-gray-400 leading-relaxed text-lg">{step.description}</p></div></div></motion.div>))}</div></div></section>); }