'use client';
import { motion, useInView } from 'framer-motion'; import { useRef } from 'react'; import { Code2, Zap } from 'lucide-react';
export default function Setup() { const ref = useRef(null); const isInView = useInView(ref, { once: true, margin: '-100px' }); return (<section ref={ref} className="relative py-24 overflow-hidden"><div className="absolute inset-0 red-grid opacity-20" /><div className="relative max-w-7xl mx-auto px-6"><div className="glass-red rounded-3xl p-8 md:p-16 text-center"><motion.div initial={{ opacity: 0, scale: 0.9 }} animate={isInView ? { opacity: 1, scale: 1 } : {}} transition={{ duration: 0.6 }}><div className="inline-flex items-center gap-2 px-4 py-1.5 bg-synabs-red/20 rounded-full mb-6"><Zap className="w-4 h-4 text-synabs-red" /><span className="text-sm font-medium text-synabs-red">Setup</span></div><h2 className="font-display font-bold text-4xl md:text-5xl lg:text-6xl mb-6">2 minute setup</h2><p className="text-xl text-gray-300 max-w-2xl mx-auto mb-8">One line of code. Your AI agent goes live instantly, whether you set it up yourself or with our help.</p><div className="max-w-2xl mx-auto mb-10"><div className="glass rounded-xl p-6 text-left font-mono text-sm"><div className="flex items-center gap-2 mb-4"><div className="w-3 h-3 rounded-full bg-red-500" /><div className="w-3 h-3 rounded-full bg-yellow-500" /><div className="w-3 h-3 rounded-full bg-green-500" /></div><code className="text-gray-300"><span className="text-synabs-red">&lt;script</span> src=<span className="text-green-400">"https://cdn.synabs.ai/agent.js"</span><span className="text-synabs-red">&gt;&lt;/script&gt;</span></code></div></div><a href="#get-started" className="btn-primary inline-flex items-center gap-2 px-8 py-4 bg-white text-black font-semibold rounded-lg hover:bg-gray-200 transition-colors">Try it free 14 days<Code2 className="w-5 h-5" /></a></motion.div></div></div></section>); }